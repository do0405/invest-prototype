[pytest]
# Avoid cacheprovider issues on Windows environments with locked temp/cache directories.
addopts = -p no:cacheprovider
norecursedirs = .git .venv venv node_modules market-intel-core pytest-cache-files-* .pytest_tmp artifacts_* provider_* _test_runtime_* results\_test_runtime_* tmp*
