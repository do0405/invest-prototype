# Core Screening Runtime Spec

## Runtime Rules

- The live behavior is owned by the listed code paths and nearest tests.
- Higher-end targets in this doc are not live unless code and tests move.

## Current Fidelity Status

- Medium fidelity; regression-tested, but the notes describe richer higher-end intent than the live rules capture.
- Higher-end deferred item: Cleaner setup grammar and better lineage between universe, pattern pool, weekly focus, and daily focus artifacts.

## Closest Tests

- `tests/test_qullamaggie_screener.py`
